    # Docker Upstart and SysVinit configuration file
    # Use DOCKER_OPTS to modify the daemon startup options.
    DOCKER_OPTS="-g /var/lib/docker --registry-mirror={{ URL }}:5000  -H tcp://0.0.0.0:4243 -H unix:///var/run/docker.sock"
